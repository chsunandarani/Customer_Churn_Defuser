services:
  - type: web
    name: customer-churn-defuser
    env: python
    buildCommand: "pip install -r requirements.txt && python dataset_generator.py && python train_model.py"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
